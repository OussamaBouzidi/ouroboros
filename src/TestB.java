import java.time.LocalDate;
import java.time.Period;
import java.util.ArrayList;
import java.util.List;
import java.util.ListIterator;
import java.util.Scanner;


public class TestB {


    public static void main(String[] args) {


/*
        Scanner sc1 = new Scanner(System.in);

        System.out.println("What is the lastname of your relative?");

        String relativeName = sc1.nextLine();

        String fictionalCity;

        int randomIndex;

        randomIndex = (int) (Math.random() * relativeName.length());


        fictionalCity = relativeName.substring(randomIndex) + relativeName.substring(0, randomIndex);

        String formatted = fictionalCity.substring(0,1).toUpperCase() + fictionalCity.substring(1).toLowerCase();

        System.out.println(formatted);



        System.out.println(randomIndex);


*/
    }

}


/*

Scanner sc = new Scanner(System.in);

        System.out.println("Type the length of the rectangle:");
        double length = sc.nextDouble();
        System.out.println("Type the breadth of the rectangle:");
        double breadth = sc.nextDouble();

        int area = (int) (length * breadth);


        System.out.println("The rectangle has the following measurements." + "\n" + "Area: " + area + "\n"
                + "Length: " + length + "\n" + "Breadth: " + breadth);
 */